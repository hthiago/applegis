{
  "name": "gabinete-funcoes",
  "description": "Ponte para as bases de execução orçamentária que não podem ser consultadas do navegador.",
  "main": "index.js",
  "private": true,
  "engines": { "node": "20" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  }
}
